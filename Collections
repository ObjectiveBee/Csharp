// Коллекции //
- для группировки связанных объектов
// в отличии от массивов, коллекции являются динамическими
// Поскольку классы коллекций организованы в рамках пространства имён, 
// они имеют множество встроенных методов для обработки коллекций
// Типы коллекций:
- Dictionary - используются для представления соединений на социальных сайтах (например, Twitter, Facebook)
- Queue - используются для создания планировщика задач
- HashSet - используются в алгоримах поиска и т.д.

// Обычно коллекция содержит методы для добавления, удаления и подсчёта объектов.
// Операторы for и foreach используются для итерации по коллекции

// Экземпляр коллекции
List<int> li = new List<int>();

/********************************************************************************************/
// Обобщённая коллекция //
- является лучшим типом коллекций, поскольку все элементы строго типизированы

// Пространство имёт System.Collections.Generic включает
- List<T>
- Dictionary<TKey, TValue>
- SortedList<TKey, TValue>
- Stack<T>
- Queue<T>
- HashSet<T>

/****************************************************************************************/

// Необобщённые коллекции //
- могут хранить элементы типа Object
// поскольку все типы данных Object могут относиться к любому типу данных,
// существует риск непредвиденного результата

// необобщённые коллекции работают медленнее, 
// а также к ним труднее получить доступ

// Пространство имён System.Collections включает следующие необобщённые коллекции:
- ArrayList
- SortedList
- Stack
- Queue
- Hashtable
- BitArray
