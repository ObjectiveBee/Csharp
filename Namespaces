// Пространства имён объявляют область, содержащую набор связанных объектов
// - можно использовать для организации элементов кода
// ключевое слово using означает, что программа использет пространства имён
using System;
...
Console.WriteLine("Hi");
// System - пространство имён, Console - класс в System

// Без выражения using, необходимо указывать пространство имён
System.Console.WriteLine("Hi");

// System является одним из примеров пространств имён .NET Фреймворка
